import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.sociobot.androidsiteblockerprivate',
  appName: 'Quietwall',
  webDir: 'dist',
  android: { backgroundColor: '#090d0c' }
};

export default config;
